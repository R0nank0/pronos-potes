{
  "name": "pronos-potes-archive",
  "version": "1.0.0",
  "description": "Archive statique du site pronos-potes.fr - 19 années d'historique",
  "main": "index.html",
  "scripts": {
    "generate-mock": "node scripts/generate-mock-data.js",
    "generate-index": "node scripts/generate-real-index.js",
    "enrich-seasons": "node scripts/enrich-seasons-data.js",
    "process-matches": "node scripts/process-matches.js",
    "process-users": "node scripts/process-users.js",
    "calculate-user-stats": "node scripts/calculate-user-stats.js",
    "process-teams": "node scripts/generate-teams-metadata.js",
    "process-pronostics": "node scripts/process-pronostics.js",
    "generate-palmares": "node scripts/generate-palmares.js",
    "update-status": "node scripts/update-season-status.js",
    "generate-all": "npm run generate-index && npm run enrich-seasons",
    "clean": "rm -rf data/",
    "regenerate": "npm run clean && npm run generate-mock",
    "regenerate-real": "npm run clean && npm run generate-all",
    "serve": "npx http-server -p 8000 -o",
    "serve:python": "python3 -m http.server 8000",
    "validate": "find data/ -name '*.json' -exec echo {} \\; | head -10",
    "stats": "echo '📊 Statistiques' && echo 'Fichiers JSON:' && find data/ -name '*.json' | wc -l && echo 'Taille totale:' && du -sh data/",
    "help": "echo '📚 Commandes disponibles:' && echo '  npm run generate-mock      - Générer les données mockées (test)' && echo '  npm run generate-all       - Générer toutes les structures réelles' && echo '  npm run enrich-seasons     - Enrichir avec les journées' && echo '  npm run process-matches    - Traiter les fichiers de matchs' && echo '  npm run process-users      - Traiter le fichier des utilisateurs' && echo '  npm run process-pronostics - Traiter les fichiers de pronostics' && echo '  npm run update-status      - Mettre à jour les statuts des saisons' && echo '  npm run serve              - Démarrer le serveur (Node)' && echo '  npm run serve:python       - Démarrer le serveur (Python)' && echo '  npm run regenerate         - Nettoyer et régénérer (mock)' && echo '  npm run regenerate-real    - Nettoyer et régénérer (réel)' && echo '  npm run stats              - Afficher les statistiques' && echo '  npm run validate           - Valider les JSON'"
  },
  "keywords": [
    "pronostics",
    "sports",
    "archive",
    "static-site",
    "ligue1",
    "football"
  ],
  "author": "Pronos-Potes",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
